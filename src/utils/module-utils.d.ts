import * as ts from 'typescript';

export declare function addProviderToNgModule(options): any;
export declare function findRootReducerFromOptions(options): any;
/**
 * Custom function to insert an NgModule into NgModule imports. It also imports the module.
 */
